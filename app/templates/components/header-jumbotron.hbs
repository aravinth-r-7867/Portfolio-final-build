<div class="container">

    {{!--Nav bar--}}
    <nav class="navbar navbar-expand-lg">
        <button onclick={{action "showNavToggle"}} class="navbar-toggler {{if showNavBarMob 'clicked'}}" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon nti__first"></span>
            <span class="navbar-toggler-icon hiddenOnClck"></span>
            <span class="navbar-toggler-icon nti__second"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end {{if showNavBarMob 'show'}}" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    {{#link-to "about" class="nav-link"}}
                        About
                    {{/link-to}}
                </li>
                <li class="nav-item">
                    {{#link-to "works" class="nav-link"}}
                        My works
                    {{/link-to}}
                </li>
            </ul>
        </div>
    </nav>
    {{!--Nav bar--}}

    <div class="header-content">
        <h1 class="display-4">Aravinth Ramesh</h1>
        <p class="lead">Web developer, photographer and travel enthusiast.</p>
    </div>
    <div class="header-backdrop"></div>
    
</div>