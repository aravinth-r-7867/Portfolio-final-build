<div class="container" id="scrollHere">
{{!--Faded backdrop--}}
<div class="header-faded-backdrop"></div>

    {{!--Nav bar--}}
    <nav class="navbar navbar-expand-lg">
        <span onclick={{action "showNavToggle"}}>Menu</span>
        <button onclick={{action "showNavToggle"}} class="navbar-toggler {{if showNavBarMob 'clicked'}}" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon nti__first"></span>
            <span class="navbar-toggler-icon nti__second"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end {{if showNavBarMob 'show'}}" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    {{#link-to "gallery.south-india" class="nav-link"}}
                        Gallery
                    {{/link-to}}
                </li>
                <li class="nav-item">
                    {{#link-to "works.remainder" class="nav-link"}}
                        My work
                    {{/link-to}}
                </li>
            </ul>
        </div>
    </nav> 
    {{!--Nav bar--}}

    <div class="header-content">
        <h1 class="display-4">Aravinth Ramesh</h1>
        <div class="lead-wrapper">
            <div class="lead" active={{leadCountOne}} onclick={{action 'leadClick' 'leadCountOne'}}>Front-end developer</div>
            <div class="lead" active={{leadCountTwo}} onclick={{action 'leadClick' 'leadCountTwo'}}>Photographer by passion</div>
            <div class="lead" active={{leadCountThree}} onclick={{action 'leadClick' 'leadCountThree'}}>Travel enthusiast</div>
        </div>
        <div class="lead-sub-header">
            {{#if leadCountOne }}
            Any doubts? I made this website from scratch
            {{else if leadCountTwo}}
            I own a Canon EOS 1500d and I love capturing landscapes 
            {{else if leadCountThree}}
            I travel a lot and love to write about them
            {{/if}}
        </div>
    </div>
    
</div>